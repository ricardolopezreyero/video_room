node_modules/
.wrangler/
.dev.vars
*.log
.DS_Store

# Pendiente: el token de gh CLI no tiene scope "workflow" todavía.
# Quitar esta línea en cuanto se otorgue el scope y se pueda pushear el workflow.
.github/
